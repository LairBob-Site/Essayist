<?php

//add_filter('the_title', 'stripTitle');

// TODO: Find a way to suppress the title output altogether -- this approach still outputs an empty div into the page
function stripTitle($text) {
    return '';
}

?>
